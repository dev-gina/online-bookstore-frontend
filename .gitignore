# Node.js 모듈 폴더 (Git에 업로드 금지)
node_modules/

# 빌드 폴더 (Next.js에서 자동 생성됨)
.next/
dist/

# 환경 변수 파일 (API 키 등 보안 관련 정보)
.env
.env.local
.env.development
.env.production
.env.test

# 로그 및 캐시 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
.cache/
out/

# TypeScript 빌드 아웃풋 (불필요한 파일 방지)
*.tsbuildinfo

# MacOS 및 Windows에서 자동 생성되는 불필요한 파일
.DS_Store
Thumbs.db
